# Legacy tray (deprecated)

PowerShell + WPF system tray and settings. Replaced by Python GUI:

- `scripts/start-engine-tray.vbs` — tray launcher
- `scripts/open-tray-settings.vbs` — settings launcher
- `engine/python/gui/` — CustomTkinter + pystray

Kept for reference only; not included in release builds.
